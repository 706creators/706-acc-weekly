name: Auto Assign Issues
on:
  issues:
    types: [opened]
jobs:
  assign:
    runs-on: ubuntu-latest
    steps:
      - name: Assign Issue
        uses: actions-ecosystem/action-auto-assign@v1
        with:
          assignees: v1xingyue
